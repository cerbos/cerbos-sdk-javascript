name: Update protos

on:
  workflow_dispatch:
  schedule:
    - cron: 0 8 * * 1

defaults:
  run:
    shell: bash

permissions: {}

jobs:
  update-protos:
    name: Update protos
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: ./.github/actions/setup-node

      - name: Update protos
        run: pnpm run proto

      - name: Generate code
        run: pnpm run generate

      - name: Create pull request
        uses: cerbos/actions/create-pull-request@025d2a858c99b1e9c062351376d0b42988c5e52c # main
        with:
          client-id: ${{ secrets.CERBOS_BOT_CLIENT_ID }}
          private-key: ${{ secrets.CERBOS_BOT_PRIVATE_KEY }}
          author: ${{ vars.CERBOS_BOT_AUTHOR }}
          branch: update-protos
          title: Update protos
          body: This PR updates protos and regenerates code.
          reviewers: haines
          add-paths: |-
            packages/*/src/protobuf/**/*.ts
            private/*/src/protobuf/**/*.ts
            proto/**/*.proto

      - name: Notify Slack on failure
        if: failure()
        uses: cerbos/actions/notify-slack@025d2a858c99b1e9c062351376d0b42988c5e52c # main
        with:
          webhook-url: ${{ secrets.SLACK_CI_NOTIFICATIONS_WEBHOOK_URL }}
