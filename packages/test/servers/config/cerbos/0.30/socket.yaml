auxData:
  jwt:
    disableVerification: true

engine:
  globals:
    allow_deletion: true

schema:
  enforcement: warn

server:
  grpcListenAddr: unix:/socket/cerbos

storage:
  driver: disk
  disk:
    directory: /policies

telemetry:
  disabled: true
